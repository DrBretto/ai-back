var gulp = require('gulp');
var merge = require('gulp-merge-json');

gulp.task('merge:json', function () {
    gulp.src('src/data/countries/*.json')
        .pipe(merge({
            fileName: 'data.json',
        }))
        .pipe(gulp.dest('./src/data/'));
})
